import Vue from 'vue';
import Vuex from 'vuex';
import cart from "./cart";

Vue.use(Vuex);

const state = {
};

const getters = {
};

const actions = {
};

const mutations = {
};

//模块
const modules = {
  cart,
};

export default new Vuex.Store({
  state,
  getters,
  actions,
  mutations,
  modules
});
